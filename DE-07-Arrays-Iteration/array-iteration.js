const prices = [100,250,50,90]
prices.forEach((fiyat)=>console.log(fiyat));

let toplam = 0;
prices.forEach((p)=>console.log(toplam +=p))
const names=["hilal", "muhammed", "guler", "sinan"]

const yeniIsim names.map ((isim)=>isim.toUpperCase();